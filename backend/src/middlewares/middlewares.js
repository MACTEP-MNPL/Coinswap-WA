import Router from 'express'

export const middlewares = new Router()

// basicMiddlewares is already applied in server.js, so we don't need it here
// Add any additional middlewares here if needed


